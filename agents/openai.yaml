interface:
  display_name: "Paper Reading"
  short_description: "Reviewer-level AI paper reading with a single self-contained Markdown report"
  default_prompt: "Use $paper-reading to analyze this AI paper with reviewer-level rigor, real references, inline figures and tables, and a single self-contained Markdown report."
