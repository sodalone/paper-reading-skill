---
name: paper-reading
description: 系统阅读、解释和批判性分析单篇 arXiv AI / 机器人论文，并生成一份“理解优先、审稿证据可追溯”的自包含 Markdown 报告。适用于 arXiv 链接、ID 或其 PDF 的精读、方法与公式讲解、实验审查、代码与论文一致性核对、相关文献定位和复现判断；默认先用直觉、例子和术语建立心智模型，再在附录保留 reviewer-level 证据。当前流水线不支持无法解析出 arXiv ID 的本地 PDF、普通 PDF URL 或 DOI 页面；不要用于纯摘要改写、无原文依据的自由发挥或多论文 survey。
---

# Paper Reading

把“帮助读者看懂”作为主路径，把“审稿、公式细节、完整表格、代码审计”作为可追溯的深度层。不要把三者混成一条从头到尾同等密度的阅读流。

## 1. 确定阅读模式

根据用户 prompt 选择模式；用户未指定时，使用“理解优先 + reviewer 附录”。

- `understand`：先讲问题、直觉、流程和关键结果；默认模式。第 5 章按需，Claim 只覆盖改变判断的主张。
- `review`：保持理解主线，但 Claim 表至少覆盖核心 novelty、主结果与最重要的因果归因；逐条检查对照公平性、统计、不一致结果和替代解释。
- `reproduce`：保持理解主线，且第 5 章必写；列出数据/权重/代码状态、关键命令与配置、训练—推理映射、资源成本、阻断项和最小可判别复现路径。

同时推断读者起点与关注点。只有不同选择会实质改变报告时才询问；否则采用：熟悉 AI 基础、不了解该论文子方向、20 分钟阅读预算。

先判定论文类型，再决定篇幅和公式密度：

- 理论论文：突出假设、定理、证明链；正文解释 2–5 个决定性公式。
- 方法论文：突出输入输出、模块关系和机制差异；正文解释 1–3 个决定性公式。
- 系统 / 平台 / 数据集 / benchmark：突出任务定义、数据、协议、实现语义和公平性；正文允许 0–2 个公式。
- 实证论文：突出因果问题、对照、统计、外推边界；不要为满足配额硬讲公式。

## 2. 先运行确定性流水线

先进入用户希望保存报告的输出根目录，再用 skill 脚本的绝对路径执行。流水线以当前目录作为输出根目录；不要在 skill 目录内运行，否则会把论文工作区写进 skill 本身。

```bash
cd "<输出根目录>"
bash "<skill 根目录>/scripts/run_pipeline.sh" "<论文链接或 arXiv ID>"
```

用户指定独立目录名时执行：

```bash
cd "<输出根目录>"
bash "<skill 根目录>/scripts/run_pipeline.sh" "<论文链接或 arXiv ID>" --workspace-name "<目录名>"
```

流水线生成唯一工作区：

```text
{workspace}/
├── {arxiv_id}_阅读报告.md
├── metadata.json
├── raw/
├── images/
└── cache/
```

只补全该主报告；不要另建平行报告。缺依赖时运行 `bash "<skill 根目录>/scripts/bootstrap.sh"`。

## 3. 阅读证据

至少检查：

- `metadata.json`
- `raw/abs.html`
- `raw/paper.pdf`
- `raw/ar5iv.html`（若存在）
- `raw/hjfy.html`
- `raw/papers_cool.html`
- `cache/references.json`
- `cache/images_manifest.json`
- arXiv 源码中的主 `.tex`、方法、实验、附录、表格和图注

若论文有公开代码且用户关心工程或复现，打开代码仓库核对关键路径；明确区分“静态代码核查”和“实际重跑结果”。

需要外部学术定位时阅读 `references/literature_search.md`；只使用已打开并核对的论文或官方页面。需要处理不确定性时阅读 `references/uncertainty_policy.md`。

## 4. 按理解顺序写报告

以 `templates/report_template.md` 为唯一结构来源。核心顺序是：

1. 三分钟读懂：问题、核心办法、主要结果、最终判断、阅读路线。
2. 先建立直觉：具体例子、方法全景、必要术语、最接近基线。
3. 方法如何工作：输入输出、核心流程、必要公式、训练与推理。
4. 证据是否成立：唯一的 Claim—Evidence—Verdict 表、主结果、消融、缺失证据。
5. 最终判断与边界：真实创新、三个关键问题、学术位置、是否值得精读/复现。
6. 工程与复现：仅在论文或用户目标需要时展开。
7. 附录：完整表格、额外数学细节、代码审计、外部文献。

正文遵循 progressive disclosure：读完前两章，读者应能回答“解决什么、怎么做、为什么可能有效”；读完第 3 章，应能回答“证据是否支持主张”。

## 5. 可读性规则

- 描述先于评价：先讲清方法，再判断 Claim；不要在读者尚未理解术语时连续给出审稿结论。
- 首次使用缩写时给出中文含义和一句话定义；术语表只保留理解主线所需的 5–10 项。
- 一个核心质疑只完整解释一次。以 Claim ID 为唯一索引，其他位置引用它，不换措辞重复。
- 正文目标为 8,000–14,000 个字符；更细内容放入同文件附录。理论论文确有必要时可超过，但要保持主线可跳读。
- 每段只完成一个论证动作；长段落拆成“判断—证据—边界”。
- 不输出内部工作标签，如“ A 级公式 / B 级公式 / 公式理解卡”。
- 不使用无评分锚点的小数分数。优先给“值得精读 / 值得浏览 / 暂不建议复现”等可行动判断；若用户要求打分，再写明评分标准。

详细写作规则见 `references/report-writing-guidelines.md`，审稿判断口径见 `references/reviewer_rubric.md`。写正文前必须读取这两个文件。

## 6. 公式、表格与图片

需要公式或实验表时读取 `references/inline-table-and-formula-template.md`。

### 公式

只在删掉公式会妨碍理解核心机制或关键证据时，才在正文深入解释。解释顺序：

1. 为什么需要它；
2. 一个直观例子或 toy example；
3. 原公式；
4. 关键符号；
5. 训练 / 推理中的作用；
6. 证据与假设边界。

数学对象使用 `$...$` 或独立 `$$ ... $$`；公式编号写在正文，不使用 `\tag{}`。能单行就单行，确需多行才用 `aligned`。代码标识符才使用反引号。

### 表格

正文只保留 2–4 张真正改变判断的精简表：

- 主结果表；
- 关键消融表；
- 必要的难度 / 泛化 / 数据规模表。

正文表格优先保留最关键列并立即解释“它验证哪条 Claim、差异多大、能否支持结论”。完整宽表放附录 A；不要在正文和附录重复同一完整表。

### 图片

优先使用 arXiv 源码中的原始 figure；失败时从原始 PDF 提取或裁剪。根据 `\includegraphics`、caption、section 和正文首次引用定位。图片紧邻它服务的解释，每张图后写 1–3 句“看什么、为什么重要”。禁止网页整页截图和文末图片区。

## 7. 证据与批判

以 Claim—Evidence—Verdict 表作为报告主骨架。每条重要 Claim 标明：

- 作者主张；
- 原文位置；
- 理论 / 主实验 / 消融 / 定性 / 外部文献证据；
- 支撑强度；
- 最大缺口；
- 报告结论。

区分作者显式主张、有原文支持的隐含结论和报告自身推断。批判要针对会改变结论的假设、对照、统计、实现或外推，不为“显得严格”罗列通用缺点。

所有关键判断必须可追溯。无法确认时写“论文未提及 / 原文证据不足 / 当前未检索到可靠文献 / 当前环境无法确认”，并说明缺少什么证据。

## 8. 完成与验证

补全报告后运行：

```bash
python3 "<skill 根目录>/scripts/validate_report_text.py" --input "<论文输入>" --root "<输出根目录>" --workspace-name "<目录名>"
python3 "<skill 根目录>/scripts/validate_report.py" --input "<论文输入>" --root "<输出根目录>" --workspace-name "<目录名>" --final
```

若未使用自定义目录名，可省略 `--workspace-name`。同一 arXiv ID 存在多个工作区时必须显式指定；验证器不得猜测或静默选择其中一个。

交付前确认：

- 三分钟摘要在正文最前面，且不依赖未定义术语；
- 方法在 Claims 审判之前被解释；
- 关键 Claim、数值、图、公式和外部文献均可追溯；
- 正文无占位符、重复整段、公式工作标签或无锚点小数评分；
- `0.6 论文与链接` 严格使用模板中的五行固定格式：中文字段名、全角冒号、直接显示完整 URL；不要改成 `[名称](URL)`，不要添加项目页、论文标题、作者或工作区；
- 图片相对路径可用；正文有关键表，附录有必要的完整数据；
- 主线和附录仍在同一个 `{arxiv_id}_阅读报告.md` 中。
